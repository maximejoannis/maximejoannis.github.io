name: Robot tests

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  robot:
    runs-on: ubuntu-latest

    services:
      selenium:
        image: selenium/standalone-chrome:4.22.0
        ports:
          - 4444:4444
        options: >-
          --shm-size=2g
        env:
          SE_OPTS: "--session-request-timeout 300 --session-timeout 300"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Wait for Selenium
        shell: bash
        run: |
          echo "Waiting for Selenium on http://localhost:4444/status ..."
          for i in {1..120}; do
            STATUS_JSON="$(curl -fsS http://localhost:4444/status 2>/dev/null || true)"
            if echo "$STATUS_JSON" | grep -q '"ready"[[:space:]]*:[[:space:]]*true'; then
              echo "Selenium is ready ✅"
              echo "$STATUS_JSON"
              exit 0
            fi
            sleep 1
          done

          echo "Selenium did not become ready in time ❌"
          echo "Last /status response (if any):"
          echo "$STATUS_JSON"
          exit 1

      - name: Run Robot tests (remote) - produce output.xml
        run: |
          mkdir -p results
          robot \
            --outputdir results \
            --output output.xml \
            --log NONE \
            --report NONE \
            -v REMOTE_URL:http://localhost:4444 \
            tests

      - name: Generate report + log with Rebot (always)
        if: always()
        run: |
          rebot --outputdir results --log log.html --report report.html results/output.xml

      - name: Diagnose Selenium service (on failure)
        if: failure()
        run: |
          echo "=== docker ps -a ==="
          docker ps -a
          echo "=== images ==="
          docker images | head
          echo "=== selenium logs ==="
          docker logs "$(docker ps -aq --filter 'ancestor=selenium/standalone-chrome:4.22.0' | head -n 1)" || true

      - name: Upload Robot reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: robot-results
          path: results
